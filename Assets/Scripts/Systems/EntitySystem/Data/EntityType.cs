namespace EntitySystem.Data
{
    public enum EntityType
    {
        Living,    // Anything that's alive (colonists, animals)
        Item,      // Pickups, resources, etc.
        Furniture  // Static objects, workstations, etc.
    }
} 